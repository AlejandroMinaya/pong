[package]
name = "pong"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["debug", "webgl2"] }
bevy_rapier2d = "0.33.0"
getrandom = { version = "0.4.2", features = ["wasm_js"] }
rand = "0.10.1"

[target.wasm32-unknown-unknown]
runner = "wasm-server-runner"
